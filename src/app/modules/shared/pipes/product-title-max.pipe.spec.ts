import { ProductTitleMaxPipe } from './product-title-max.pipe';

describe('ProductTitleMaxPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductTitleMaxPipe();
    expect(pipe).toBeTruthy();
  });
});
