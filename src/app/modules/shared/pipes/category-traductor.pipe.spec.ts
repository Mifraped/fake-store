import { CategoryTraductorPipe } from './category-traductor.pipe';

describe('CategoryTraductorPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryTraductorPipe();
    expect(pipe).toBeTruthy();
  });
});
