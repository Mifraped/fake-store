import { CategoryRoutingPipe } from './category-routing.pipe';

describe('CategoryRoutingPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryRoutingPipe();
    expect(pipe).toBeTruthy();
  });
});
